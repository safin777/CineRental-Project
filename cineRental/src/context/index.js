import { createContext } from 'react' //import the createContext function from the react library

const MovieContext = createContext() //create a context object

const ThemeContext = createContext() //create a context object

export { MovieContext, ThemeContext } //export the context object
